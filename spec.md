# CyberCore 仕様書

## 概要

Macのメニューバーに常駐し、システムリソース（CPU、メモリ、ストレージ、ネットワーク）をリアルタイム監視するデスクトップアプリケーション。80-90年代のCRTモニター風のレトロデザインが特徴。

---

## 機能要件

### メニューバー
- **アイコン**: 静的アイコンを表示（リアルタイム数値表示なし）
- **クリック時**: ポップアップウィンドウ（BrowserWindow）を表示

### 表示項目
| 項目 | 内容 |
|------|------|
| CPU使用率 | 現在のCPU使用率（%） |
| メモリ使用量 | 使用中/総容量（GB） |
| ストレージ容量 | 使用中/総容量（GB） |
| OS基本情報 | macOSバージョン、ホスト名など |
| ネットワーク | リアルタイム転送量（↑↓ MB/s）、接続状態、IPアドレス |

### 表示形式
- **数値 + プログレスバー**でデータを視覚化
- レトロデザインと相性の良いドット風プログレスバーを採用

### データ更新
- `setInterval`で **1〜2秒ごと** にシステム情報を取得・更新

---

## UI / UX 仕様

### ウィンドウ
- **タイプ**: Tray連動のポップアップBrowserWindow
- **サイズ**: 約400 x 350px（中程度）
- **位置**: メニューバーアイコン直下に表示

### デザインコンセプト
- **テーマ**: ヴィンテージ / ノスタルジック（80-90年代コンピュータUI）
- **フォント**: ピクセルアートフォント（Press Start 2P、VT323など）
- **アニメーション**: 点滅するテキスト、カーソル

### CRTモニター風エフェクト（中程度）
CSSベース + 軽量Canvas/SVGアニメーションで実装:
- スキャンライン（横線オーバーレイ）
- グロー効果（テキストの発光）
- 軽微なノイズ/ちらつき
- レトロなボーダー・パネルデザイン

※ WebGLシェーダーによる画面湾曲・色収差などの高負荷エフェクトは不採用

---

## 技術仕様

### アーキテクチャ
```
┌─────────────────────────────────────────────────────┐
│ Main Process (Node.js)                              │
│ - Tray管理                                          │
│ - BrowserWindow制御                                 │
│ - システム情報取得（systeminformationライブラリ）    │
│ - IPC通信でRendererへデータ送信                     │
└─────────────────────────────────────────────────────┘
                         ↓ IPC
┌─────────────────────────────────────────────────────┐
│ Renderer Process (React)                            │
│ - UIコンポーネント                                  │
│ - CRTエフェクト（CSS + Canvas）                     │
│ - データ表示・更新                                  │
└─────────────────────────────────────────────────────┘
```

### 主要ライブラリ
| 用途 | ライブラリ |
|------|-----------|
| システム情報取得 | `systeminformation` |
| フォント | Google Fonts（Press Start 2P, VT323） |

### 設定の永続化
- **初期段階では不要**（electron-storeは必要になり次第追加）

---

## 懸念点・トレードオフ

### パフォーマンス
- 1-2秒ごとの更新 + CRTエフェクトによるCPU/GPU負荷
- 対策: CSSベースのエフェクトを中心に採用し、負荷を抑制

### macOS権限
- システム情報取得に特別な権限は不要（systeminformationで取得可能な範囲）
- ネットワークトラフィック監視は標準APIで対応

### バッテリー消費
- 常駐アプリのため、更新頻度がバッテリーに影響
- 将来的に更新間隔の設定機能を検討

---

## 将来の拡張（スコープ外）
- 更新間隔のカスタマイズ
- 表示項目のオン/オフ設定
- macOS起動時の自動起動
- 通知機能（CPU/メモリが閾値を超えた場合）
